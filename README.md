# NP Betting — Frontend (Vite + React)

Simple demo frontend for NP Betting. Uses Firebase client (Auth + Firestore).
**Important:** This demo runs the draw on the client. For production use server-side draw (Cloud Function).

## Setup (local)
1. Install dependencies:
   ```bash
   npm install
